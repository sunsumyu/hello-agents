# ============================================================
# 智能API测试助手 - 依赖清单
# ============================================================

# 核心依赖
hello-agents>=1.0.0

# HTTP 请求：真实调用目标接口
requests>=2.31.0

# OpenAPI 文档解析（YAML 格式）
PyYAML>=6.0

# 环境变量加载（读取 .env 里的 LLM 配置）
python-dotenv>=1.0.0

# JSON Schema 校验：用来验证接口返回的数据结构是否符合文档约定
jsonschema>=4.19.0

# HTML 报告模板引擎：用来生成漂亮的测试报告网页
jinja2>=3.1.0

# Web 服务：把测试能力暴露成 HTTP 接口（前端需要）
fastapi>=0.104.0
uvicorn>=0.24.0
